name: Postman Regression - Grupo 07 Carrito (con reporte PDF en Python)

on:
  push:
    branches: ["main"]
    paths:
      - "postman/C_grupo07_carrito_ecomerce_Armin_Avezada.postman_collection.json"
      - ".github/workflows/postman-grupo07-armin-regression.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "postman/C_grupo07_carrito_ecomerce_Armin_Avezada.postman_collection.json"
      - ".github/workflows/postman-grupo07-armin-regression.yml"
  workflow_dispatch:

jobs:
  newman-regression:
    name: Armin - Newman + reporte PDF (Python)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Configurar Python (generador de PDF)
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias Node
        run: npm install

      - name: Instalar dependencias Python (reportlab, Pillow)
        run: pip install -r skills/postman-newman-skill/reporter/requirements.txt

      - name: Ejecutar regresión Postman (Newman) - Grupo 07
        run: >
          npx newman run "postman/C_grupo07_carrito_ecomerce_Armin_Avezada.postman_collection.json"
          --env-var "baseUrl=${{ vars.GRUPO07_BASE_URL }}"
          --env-var "x-api-key=${{ secrets.GRUPO07_API_KEY }}"
          --delay-request 800
          --reporters cli,json
          --reporter-json-export newman/results.json

      - name: Generar reporte PDF
        if: always()
        run: >
          python skills/postman-newman-skill/reporter/newman_report.py
          --results newman/results.json
          --banner skills/postman-newman-skill/reporter/assets/banner_portada.png
          --logo-aiquaa skills/postman-newman-skill/reporter/assets/logo_aiquaa_circle.png
          --logo-postman skills/postman-newman-skill/reporter/assets/logo_postman_clean.png
          --repo-url "${{ github.server_url }}/${{ github.repository }}"
          --api-version "${{ github.sha }}"
          --author "${{ github.triggering_actor }}"
          --run-url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          --output newman/report.pdf

      - name: Subir informe PDF de la regresión
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: informe-regresion-grupo07
          path: newman/report.pdf
          retention-days: 7